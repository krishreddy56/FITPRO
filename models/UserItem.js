class UserItem{
  constructor(Item, Rating, Status, SwapItem, SwapItemRating, SwapperRating,Swapby){
    this.Item=Item;
    this.Rating=Rating;
    this.Status=Status;
    this.SwapItem=SwapItem;
    this.SwapItemRating=SwapItemRating;
    this.SwapperRating=SwapperRating;
    this.Swapby=Swapby
}
}

module.exports=UserItem;
